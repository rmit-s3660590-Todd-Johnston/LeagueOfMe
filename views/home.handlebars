{{info.name}} has an id of {{info.id}} and is ranked: {{info.tier}} {{info.rank}}
and his favourite champion is {{info.champName}}
 <style>
        body {
            background-image:url({{info.champIcon}});
            background-repeat: repeat;
            background-position: right top;
            color: aliceblue;
            text-align: center;
        }
 </style>